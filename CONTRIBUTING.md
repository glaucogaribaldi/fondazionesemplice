# Contributing

1. Keep paper mode as the default.
2. Never add exchange credentials, `.env`, model weights or runtime data.
3. Add tests for every Risk Engine change.
4. Preserve fail-closed behavior.
5. Run `make validate` and `make test` before opening a pull request.
6. Treat model, dependency and container upgrades as behavior changes requiring paper revalidation.
